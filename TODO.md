# TODO

Release packaging is complete for the current Windows x64, Linux x64, and macOS arm64 prebuilt overlays, plus WebAssembly npm packages for frontend bundlers. Remaining follow-up work:

- Broaden numerical regression tests across additional robot and sheet configurations.
- Expand documentation for algorithm details and expected numeric tolerances.
- Add a Web Worker example for high-frequency browser simulation loops.
- Track WebAssembly package size and consider size-oriented release optimizations if frontend users need smaller downloads.
- Consider adding additional prebuilt triplets if demand grows beyond the current Windows x64, Linux x64, and macOS arm64 coverage.
